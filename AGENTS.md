# 项目级规则

1. **回答与文档语言**：所有回答和文档输出必须使用中文。

2. **打包限制**：只允许构建 `release` 包 并且默认只构建-Plocal.install.abi=arm64-v8a；禁止构建 `debug` 或其他非 `release` 包。除非用户明确可以直接打包安装，不然执行任何打包命令前，必须先输出拟执行命令并等待用户明确确认。

3. **修改确认**：进行任何代码、配置或文档修改前，必须先输出修改方案并等待用户明确确认。
